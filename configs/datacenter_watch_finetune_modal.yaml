project_name: "datacenter_watch"
model_name: "lfm2.5-VL-450M"
training_type: "vlm_sft"

dataset:
  path: "/outputs/data/datacenter_watch/datacenter_watch_train.jsonl"
  type: "vlm_sft"
  image_root: "/outputs/data/datacenter_watch/images"

training_config:
  extends: "DEFAULT_VLM_SFT"
  num_train_epochs: 3
  per_device_train_batch_size: 2
  gradient_accumulation_steps: 8
  learning_rate: 2e-5
  lr_scheduler_type: "cosine"
  warmup_ratio: 0.03
  seed: 42
  eval_on_start: false
  eval_strategy: "epoch"
  save_strategy: "epoch"
  logging_steps: 5
  tracker: "trackio"
  trackio_space_id: "Paulescu/datacenter_watch-finetune"

peft_config:
  extends: "DEFAULT_VLM_LORA"
  use_peft: false

benchmarks:
  image_root: "/outputs/data/datacenter_watch/images"
  benchmarks:
    - name: "wildfire_eval"
      path: "/outputs/data/datacenter_watch/datacenter_watch_test.jsonl"
      metric: "short_answer"
      max_new_tokens: 256

modal:
  app_name: "datacenter_watch"
  gpu: "H100:1"
  timeout: 7200
  output_volume: "datacenter_watch"
  output_dir: "/outputs"
  detach: false
